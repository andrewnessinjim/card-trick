import styled from "styled-components";

export const Button = styled.button`
  width: 72px;
  aspect-ratio: 1 / 1;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-size: 16px;
`;
export default Button;
